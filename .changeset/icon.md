---
"pixel-perfect": minor
---

Add `Icon` (1.3): an SVG wrapper with `1em` sizing, `currentColor`, and a required `label` or `decorative` at the type level. Adds the `--pp-size-*` element-size scale to the token layer.
